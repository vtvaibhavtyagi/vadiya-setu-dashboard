import React from "react";
import { Particles } from 'react-particles';
import particleConfig from "./particleConfig";

export default function Particle() {
  return <Particle params={particleConfig}></Particle>;
}
